import { AbsolutePathPipe } from './absolute-path.pipe';

describe('AbsolutePathPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsolutePathPipe();
    expect(pipe).toBeTruthy();
  });
});
