import { AppendImagePathPipe } from './append-image-path.pipe';

describe('AppendImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
